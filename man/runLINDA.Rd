% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runLINDA.R
\name{runLINDA}
\alias{runLINDA}
\title{Performs the LINDA analysis once the inputs are given.}
\usage{
runLINDA(
  input.scores = input.scores,
  as.input = NULL,
  background.network = bg,
  solverPath = NULL,
  input.node = NULL,
  constraints_mode = "hard",
  pValThresh = 0.05,
  splice_effect_sign = "negative",
  top = 50,
  lambda1 = 100,
  lambda2 = 1,
  mipgap = 0,
  relgap = 0,
  populate = 500,
  nSolutions = 100,
  timelimit = 3600,
  intensity = 1,
  replace = 1,
  threads = 0,
  condition = 1,
  solver = "cplex",
  save_res = TRUE
)
}
\arguments{
\item{input.scores}{A data-frame of two columns containing the estimated
transcription factor activities (TF). The data-frame should have columns ID's
as \code{c('id', 'nes')}, indicating to the TF ID's and the corresponding
inferred enrichment score.}

\item{as.input}{A data-frame containing results from the alternative splicing/
exon skipping analysis. It should contain at least three columns named
\code{id}, \code{effect}, and \code{significance}. By default,
\code{as.input = NULL}, which means that LINDA will perform a splice-unaware
inference of upstream regulatory networks.}

\item{background.network}{Data frame of the prior knowledge network of
protein-protein (PPI) and domain-domain (DDI) interactions. The data-frame
should contain at least 6 columns named \code{id_source}, \code{id_target},
\code{pfam_source}, \code{pfam_target}, \code{gene_source}, and
\code{gene_target}.}

\item{solverPath}{Optional location path to the desired solver executable.
Required when \code{solver} is \code{"cplex"} or \code{"cbc"}.}

\item{input.node}{Optional vector of perturbation inputs from where the protein
interactions start. By default \code{input.node = NULL}, in which case an
auxiliary \code{Perturbation} node is added and connected on top of all uppermost
nodes in the background network.}

\item{constraints_mode}{Character. Defines how alternative-splicing evidence is
integrated into the ILP. Use \code{"hard"} to add AS constraints that force
significantly affected reactions to zero. Use \code{"soft"} to keep reactions
feasible but penalize them in the objective function. Default is
\code{"hard"}.}

\item{pValThresh}{Optional p-value/FDR threshold used in
\code{constraints_mode = "hard"} to define significantly affected
exons/transcripts. AS-affected reactions passing this threshold and the selected
\code{splice_effect_sign} are excluded from the feasible ILP solution through
the AS constraints. By default, \code{pValThresh = 0.05}. This parameter is not
used for \code{constraints_mode = "soft"}.}

\item{splice_effect_sign}{Character. Defines which sign of the AS effect is
considered functionally relevant. It can be \code{"negative"},
\code{"positive"}, or \code{"both"}. Default is \code{"negative"}.}

\item{top}{Optional parameter indicating the number of TF's to be considered as
significantly regulated based on the absolute highest activity value. When
considering all TF's, use \code{top = "all"}. Default is \code{top = 50}.}

\item{lambda1}{Penalization term of the primary objective of the objective
function - TF inclusion. Default is \code{lambda1 = 100}.}

\item{lambda2}{Penalization term of the secondary objective of the objective
function - size penalty. In \code{constraints_mode = "soft"}, this term is
additionally scaled by AS-derived penalties. Default is \code{lambda2 = 1}.}

\item{mipgap}{CPLEX parameter which sets an absolute tolerance on the gap between
the best integer objective and the objective of the best node remaining. Default
is \code{mipgap = 0}.}

\item{relgap}{CPLEX parameter which sets a relative tolerance on the objective
value for the solutions in the solution pool. Default is \code{relgap = 0}.}

\item{populate}{CPLEX parameter which sets the maximum number of MIP solutions
generated for the solution pool during each call to the populate procedure.
Default is \code{populate = 500}.}

\item{nSolutions}{The number of solutions to be provided by LINDA. Default is
\code{nSolutions = 100}.}

\item{timelimit}{CPLEX parameter which sets the maximum optimization time in
seconds. Default is \code{timelimit = 3600}.}

\item{intensity}{CPLEX parameter which controls the trade-off between the number
of solutions generated for the solution pool and the amount of time or memory
consumed. Values from 0 to 4 invoke increasing effort. Default is
\code{intensity = 1}.}

\item{replace}{CPLEX parameter which designates the strategy for replacing a
solution in the solution pool when the solution pool has reached its capacity.
Default is \code{replace = 1}.}

\item{threads}{CPLEX parameter which manages the number of threads that CPLEX
uses. Default is \code{threads = 0}, letting CPLEX decide.}

\item{condition}{Parameter used when LINDA is run over multiple analyses in
parallel. It helps distinguish between multiple ILP problems and their
solutions. Default is \code{condition = 1}.}

\item{solver}{Character parameter indicating the type of solver to be used. It
can take \code{"lpSolve"}, \code{"cplex"}, or \code{"cbc"}. Default is
\code{"cplex"}.}

\item{save_res}{Logical. If \code{TRUE}, save the result object as
\code{paste0("res_", condition, ".RData")}. Default is \code{TRUE}.}
}
\value{
Results list containing each of the LINDA unique solutions as well as the
combined ones.
}
\description{
Performs the LINDA analysis once the inputs are given.
}
\details{
LINDA can integrate alternative-splicing evidence in two ways. In
\code{constraints_mode = "hard"}, significant AS-affected reactions are excluded
from the feasible solution by AS constraints. In \code{constraints_mode = "soft"},
the same AS evidence is encoded as an objective-function penalty, so affected
reactions remain feasible but are discouraged.
}
\examples{
library(LINDA)
library(igraph)
library(XML)

load(file = system.file("extdata", "as_data_toy.RData", package = "LINDA"))
load(file = system.file("extdata", "bg_toy.RData", package = "LINDA"))
load(file = system.file("extdata", "tf_act_toy.RData", package = "LINDA"))

\dontrun{
res_hard <- runLINDA(input.scores = input.scores,
                     as.input = as.input,
                     background.network = bg,
                     solverPath = "~/Downloads/cplex",
                     input.node = NULL,
                     constraints_mode = "hard",
                     pValThresh = 0.05,
                     splice_effect_sign = "negative",
                     top = 2,
                     lambda1 = 10,
                     lambda2 = 0.001,
                     mipgap = 0.001,
                     relgap = 0.001)

res_soft <- runLINDA(input.scores = input.scores,
                     as.input = as.input,
                     background.network = bg,
                     solverPath = "~/Downloads/cplex",
                     constraints_mode = "soft",
                     splice_effect_sign = "negative",
                     top = 2,
                     lambda1 = 10,
                     lambda2 = 0.001)
}
}
